# flutter_app/.rule

> Canonical rule file for `flutter_app/`. `AGENTS.md` and `CLAUDE.md` are compatibility symlinks; edit this file instead.

## 当前定位

`flutter_app` 是 Rabbit 管理系统的并行 Flutter Android 客户端。它用于承接首版 Flutter 重构，不替换也不删除现有 `../android` 原生 Java App，直到用户明确要求切换。

首版范围保持聚焦：

- 登录 / 注册
- 会话恢复
- 兔舍列表、创建和选择
- 首页四栏导航
- 今日预警
- 生产流程 Tab
- 空态、加载态、错误态
- 未迁移模块的清晰占位入口

除非任务明确要求，不迁移 NFC、后台通知、离线待提交、全量批次操作页，也不修改后端 schema。

## 工程边界

- 不要为了 Flutter UI 工作修改 `../android`，除非任务明确要求原生 Android 联动。
- 不要为了 Flutter UI 工作修改 `../backend` 的 API 契约，除非任务明确要求后端支持。
- 新增 Flutter 功能时优先沿用本目录已有分层和命名，不引入另一套架构。
- 保持 Android-only 首版，不新增 iOS/Web/macOS/Linux/Windows 平台文件，除非用户明确要求。

## 技术栈与目录职责

- 路由使用 `MaterialApp.router` + `go_router`，路由定义集中在 `lib/src/routing/routes.dart`。
- 状态管理使用 `flutter_riverpod`。异步状态放在 provider/controller/view model，不放在深层 widget 的临时状态里。
- API 访问统一通过 `lib/src/data/services/network/client.dart` 的 `dio` 封装，不在页面局部创建新的 `Dio`。
- 会话持久化逻辑放在 `lib/src/data/services/auth/session.dart`。
- token 使用 `flutter_secure_storage` 保存。
- `userName`、`userId`、`currentHouseId`、`baseUrl` 等非敏感信息使用 `shared_preferences`。
- 视觉 token 和主题集中在 `lib/src/ui/core/theme.dart`，不要在业务 widget 里散落一次性 hex color。

## Flutter 官方架构组织方式

`flutter_app` 按 Flutter 官方 App Architecture 推荐的轻量分层组织：`UI layer`、`Data layer`、共享 `domain models`、路由和配置分离。新增代码必须沿用这套结构，不再新增旧式 `features/`、`models/`、`core/`、`shared/`、`theme/` 顶层目录。

目录职责：

- `lib/src/config/`：应用级配置和运行时常量。
- `lib/src/data/repositories/<business>/`：按业务归档的 Repository，负责组织后端数据、请求参数和 DTO 转换。
- `lib/src/data/services/<capability>/`：按技术能力归档的底层服务，例如 `network/`、`auth/`、`nfc/` 和 `storage/`；页面不得直接绕过 repository 调用 service。
- `lib/src/domain/<business>/`：按业务归档、跨 UI 和 Data 层复用的数据模型。保持纯数据和轻量解析逻辑，不放 UI 或网络请求。
- `lib/src/routing/`：路由表、导航守卫、ShellRoute 等导航结构。
- `lib/src/ui/<feature>/screens/`：可由路由或 Navigator 打开的完整页面，例如 `ui/houses/screens/list.dart`。
- `lib/src/ui/<feature>/sheets/`：通过 bottom sheet 或 dialog 呈现的业务表单与流程。
- `lib/src/ui/<feature>/widgets/`：只放该业务内部复用、不能独立导航的界面组件。
- `lib/src/ui/<feature>/view_models/`：该功能需要显式 UI 状态协调时放 ViewModel / Controller，例如 `ui/auth/view_models/controller.dart`。
- `lib/src/ui/core/widgets/`：跨功能复用的页面壳、空态、错误态、加载态等 UI 组件。
- `lib/src/ui/core/theme.dart`：主题、颜色和视觉 token。

Data 和 Domain 层使用业务能力命名；`home`、`dashboard` 这类界面名称只属于 UI 层，
对应的底层业务分别归入 `reproduction`、`reports` 等实际业务命名空间。

共享代码放在最近的职责边界：网络响应校验归 `data/services/network/`，跨业务 UI
原语归 `ui/core/widgets/`。只有两个以上真实调用方且契约稳定时才提取；不要新增
`shared/`、`common/`、`utils/` 这类无法表达所有权的杂物目录。

新增功能时的默认落点：

- 单 Repository 业务的后端接口封装放到 `data/repositories/<business>/repository.dart`；同一业务有多个 Repository 时用 `account.dart`、`session.dart` 等职责名区分。
- 后端返回结构或请求展示所需模型放到 `domain/<business>/`。
- 完整页面放到 `ui/<feature>/screens/`，临时业务窗口放到 `sheets/`，页面片段才放到 `widgets/`。
- 复杂 UI 状态放到 `ui/<feature>/view_models/`；简单页面可以只用 provider 和局部表单状态。
- 路由只在 `routing/routes.dart` 增加。
- 只有当业务规则明显复杂到 repository/view model 难以承载时，才引入额外 domain service/use case；不要提前空转抽象。

文件命名将父目录视为命名空间：

- 文件名只表达父目录内的区分语义，例如 `ui/rabbits/screens/detail.dart`、`ui/rabbits/sheets/move.dart`。
- 不重复父目录已经表达的角色，禁止 `_screen.dart`、`_sheet.dart`、`_repository.dart`、`_service.dart`、`_store.dart`、`_provider.dart`、`_providers.dart`、`_controller.dart`、`_config.dart`、`_theme.dart`、`_model.dart` 和 `_models.dart`。
- 列表入口用 `list.dart`，模块总览用 `overview.dart`，不要使用无法表达用途的 `index.dart`。
- 领域主实体保留自然名称，例如 `domain/rabbits/rabbit.dart`；不要为了消除业务词而改成含义模糊的 `model.dart`。

## 测试目录职责

`test/` 沿用源码的职责边界，不在根目录平铺测试文件：

- 应用装配、启动、会话失效和路由生命周期放在 `test/app/`。
- 配置和静态文案放在 `test/config/`。
- Repository 测试放在 `test/data/repositories/<business>/`，底层服务测试放在 `test/data/services/<capability>/`。
- 纯模型和业务规则测试放在 `test/domain/<business>/`。
- 路由表、导航守卫和 ShellRoute 测试放在 `test/routing/`。
- UI 测试放在 `test/ui/<feature>/{screens,sheets,widgets,view_models}/`，与被测源码的界面类型一致。
- 架构约束放在 `test/architecture/`；真机流程放在 `integration_test/<business>/`。

测试跨越多层时，按最外层被测行为归档。页面测试即使替换了 Repository，仍归页面；验证请求和响应契约的测试即使解析了领域模型，仍归 Repository。测试辅助代码放在最近的职责目录，不新增 `shared/`、`common/` 或 `utils/` 杂物目录，也不为没有测试的源码目录创建空镜像。

## API 规则

- Android 模拟器默认后端地址为 `http://10.0.2.2:8080`。
- 后端地址通过 Flutter 编译期变量 `RABBIT_API_BASE_URL` 注入，配置文件放在 `config/env/`。
- AppConfig 读取优先级为 `--dart-define` / `--dart-define-from-file`、已打包的 `config/env/dev.env` 或 `config/env/test.env` asset、最后才是代码兜底默认值。
- 不要使用 Android Studio Run Configuration 的 `Environment variables` 配置 `RABBIT_API_BASE_URL`；Flutter Android 运行时不会把它当成 `String.fromEnvironment`。
- 脚本层环境固定为 `dev` / `test` / `prod`；Android flavor 固定为 `dev` / `staging` / `prod`，其中 `test` 环境映射到 `staging` flavor，正式 `prod` flavor 的包名保持 `com.rabbit.app.flutter`。
- `dev` 使用 `config/env/dev.env`；`test` 使用 `config/env/test.env`；`prod` 使用本地创建的 `config/env/prod.env`，不要提交真实正式环境配置。
- 旧的 `release` 环境名仅作为脚本兼容别名，映射到 `prod` flavor 和本地 `config/env/release.env`。
- 打包时优先使用 `./rabbit apk <dev|test|prod> --release`，或直接传 `--flavor dev|staging|prod --dart-define-from-file=config/env/<env>.env`。
- `dev` / `test` / `prod` 表示脚本层后端环境，`dev` / `staging` / `prod` 表示 Android flavor，`--debug` / `--profile` / `--release` 表示 Flutter 构建模式，三者不要混用。
- 登录后请求统一带 `Authorization: Bearer <token>`。
- 兔舍域请求统一带 `X-House-Id: <houseId>`。
- 后端包装响应中 `ApiResponse.code != 0` 必须视为业务失败，并显示明确错误文案。
- 超时、断网、401、无兔舍、无预警、API code 非 0 都要有明确 UI 状态。

首版已接入或计划接入的接口：

- `POST /api/auth/login`
- `POST /api/auth/register`
- `GET /api/houses`
- `POST /api/houses`
- `GET /api/events?onlyUnnotified=true`

需要幂等的写请求使用 `uuid` 生成 `requestId`。生产 UI 不写死 mock 响应；无数据时使用空态或错误态。

## UI 规则

- 首页信息结构对齐截图：居中标题、兔舍上下文、`今日预警!`、生产流程 Tab、系统使用步骤空态、四栏底部导航。
- 视觉方向是专业养殖管理工具，不做营销页、落地页或装饰型 hero。
- 顶部保留居中标题；右侧操作区使用 Flutter 原生操作，包括兔舍切换、刷新、设置等。
- 今日预警区域展示主标题、预警数量、逾期 / 到期 / 未到期状态 chips。
- 底部导航固定四栏：`首页`、`兔舍`、`数据面板`、`我的`。
- 未完成页面使用可点击占位页，说明模块状态并保留后续入口，避免用户以为功能消失。

视觉 token：

- 背景：`#F7F8FA`
- 主蓝：`#1577D2`
- 预警红：`#EF4444`
- 田野绿：`#2F8F6B`
- 提醒琥：`#F59E0B`
- 墨色：`#17202A`
- 辅助文字：`#667085`
- 边线：`#E5E8EC`
- 浅底色（依次对应蓝/绿/琥/红）：`#EAF4FF`、`#EAF7F1`、`#FFF7E6`、`#FFEFEF`

笼位关注度色板（分层地图专用，代码位于 `ui/cages/widgets/map.dart`，
`CageAttentionStyle` 的语义定义在 `domain/cages/attention.dart`）：

| 状态 | 含义 | 颜色 | 图标 |
| --- | --- | --- | --- |
| 异常 | 账不平（空笼却记着在栏、单兔笼超载、商品笼超上限、停用笼里还有兔） | 预警红 | `error_outline` |
| 停用 | 已停用且笼内无兔 | 辅助灰 | `block` |
| 待投喂 | 有兔且今日未投喂 | 提醒琥 | `schedule` |
| 已满 | 放不下了 | 墨色（中性） | `remove_circle_outline` |
| 有空位 | 还能放兔，含空笼 | 田野绿 | `add_circle_outline` |

两条不能破的约束：

- **主蓝不得用于任何笼位状态**。地图同时充当换笼选择器，主蓝只表示“已选中”；
  若给状态用了蓝，选中态和普通态就分不开。
- **颜色不得是唯一区分手段**。每个状态必须同时带图标与文字（图例 + 无障碍标签），
  色觉障碍用户不能因为分不清红绿就读不出笼位状态。

交互和布局约束：

- 主要圆角使用 8px，除非已有组件需要其它半径。
- 控件图标优先使用 Material icons。
- 不添加装饰渐变、过大的 hero section、纯装饰插画或模板化背景。
- 长兔舍名、用户名、事件名称必须设置 `maxLines` 和 `TextOverflow.ellipsis`。
- 文案不能在 360x800 和 412x915 常见 Android 尺寸下重叠或撑破容器。
- 界面不出现英文领域名词。生产容器在界面上叫「批次」，不叫 `Batch`；
  `Batch` 只活在类名、provider、接口字段和幂等指纹里。养兔的人读中文，写代码的人读标识符。
- 招人入兔舍用的那串唯一标识（形如 `R3F9A0C21B7`）在界面上叫「账号」，不叫「兔号」：
  「兔号」已经被兔只编号占着（批次、搜索框里都是这个意思），两个意思撞名会让人拿错号。
  代码里仍然叫 `userCode`。
- 固定格式控件，如 Tab、底部导航、统计 chips、工具按钮，需要稳定尺寸，避免加载或 hover/focus 状态导致布局跳动。
- 笼位分层地图的结构是「层 → 排 → 位」，**层是切换出来的空间，不是叠在一起的格子**。
  现场的多层笼是错位的阶梯，人站在某一层前面时眼里只有这一层的那几排；
  把几层画成剖面图看着信息全，找笼时反而对不上眼前的架子。地图一次只画一层。
- **笼位编号一律是「排-位-层」**，例如 `2-3-1`；顺序照人在舍里走的路线：
  先找到那一排，再沿排数到第几位，最后抬头看第几层。客户端不要自己拼编号——
  批量建笼只上报 rowCode/positionIndex/layerIndex，编号由后端 `CageNumbers` 生成，
  跟建兔舍自动铺的笼位同一套。角落里没有规整坐标的零散笼位才手填编号。
- **层号 1 是最下面那一层**：现场从地面往上数。凡是要把层写成字的地方
  （批量建笼的编号），两层是「下/上」、三层是「下/中/上」，再多就写「第 N 层」；
  层切换器按 1、2、3 顺着排。
- 笼位编号里的尾数是**位号**，不是流水号。写成流水号的话，两层时第 2 位会拿到 3，
  地图格子写「2」、笼上的签写「3」，人对实物必错。
- 一排就是一条线：位从左往右按位号递增，排从上往下按排号递增（见 `CageLayout`）。
  窄屏下整排横向滚动，不换行——换行会让「第几位」错位。
- 切层会把别的层整个藏起来，所以层签上必须带该层「要处理的笼」数量（异常 + 待投喂）；
  选中的笼在别的层时（碰标签、输编号）地图要自己切过去，
  不能让人看到「已选中」却在屏上找不到那一格。
- 缺笼的坐标留空槽，不得把后面的笼往前挤，否则“第几排第几位”就不可信；
  格子上写位号（排号在排头、层号在切换器，三者拼起来就是笼位）。
- 筛选和搜索在地图上只能压暗不命中的格子，不能把它们从网格里移除。
- 笼位管理的地图格子以在栏种母兔的 `currentStage` 显示生产阶段；
  没有种母兔或母兔尚无生产阶段时显示「无状态」。在栏数仍保留在笼位统计和无障碍语义中。
- 笼位多时按「排」分页，不一次铺完；大字号下格子跟着长，不用 `FittedBox` 缩字。
- 笼位管理只保留分层地图，不提供列表视图切换；搜索和筛选只压暗不命中的格子，不能改变空间坐标。

## Android 构建规则

- 正式 `prod` flavor 的 Android package id 保持为 `com.rabbit.app.flutter`，避免覆盖现有原生 App。
- `dev` / `staging` flavor 分别使用 `com.rabbit.app.flutter.dev` / `com.rabbit.app.flutter.test`，方便和正式包在同一设备并存。
- 正式 Android app label 保持为 `鸿兔智管`；`dev` / `staging` label 使用 `鸿兔智管 Dev` / `鸿兔智管 Test`。
- `pubspec.yaml` 的 `flutter.default-flavor` 保持为 `dev`，方便 Android Studio 和 Flutter 工具默认进入本地开发 flavor。
- `prod` flavor 的 `usesCleartextTraffic` 必须保持关闭；本地 `dev` / `staging` 可以开启以支持 `10.0.2.2` 或局域网 HTTP 调试。
- 共享 Android Studio Run/Debug Configurations 只提交 `.run/*.run.xml`，不要提交 `.idea/workspace.xml`、设备缓存或个人 IDE 状态。
- 共享配置名称保持为 `Rabbit Dev` / `Rabbit Test` / `Rabbit Release` / `Rabbit Tests`。
- Android 构建统一使用 JDK 21；不要在 tracked `android/gradle.properties` 中写死某台机器的 `org.gradle.java.home`。
- 首次配置运行 `./rabbit bootstrap`。工具链脚本按当前环境变量、本地 `config/env/toolchain.local.env`、项目配置、`PATH` 和常见安装位置动态解析 Flutter、JDK 21、Android SDK，并同步 Flutter `jdk-dir` 与 Android Studio 的 `GRADLE_LOCAL_JAVA_HOME`。
- 日常开发、检查、测试、构建和发布优先通过 `./rabbit`；Android 相关命令必须经工具链脚本动态注入 Java 和 SDK，不提交机器绝对路径。
- Android Studio 的 Gradle JDK 选择 `GRADLE_LOCAL_JAVA_HOME`；IDE 本身仍可使用随 Android Studio 安装的 JBR。

- Gradle wrapper 使用 `8.4`。
- Android Gradle Plugin 使用 `8.2.2`。
- Kotlin Android plugin 使用 `1.9.22`。
- NDK 使用 `25.1.8937393`。
- 不切换到 Gradle 9.x，除非构建失败证明必须升级并同步处理 AGP 兼容。
- 如果 Android Studio 与终端 Gradle 表现不同，优先单独排查 IDE JDK / Gradle 配置，不要盲目改项目版本。

## 常用命令

在 `flutter_app/` 下：

```bash
./rabbit bootstrap
./rabbit analyze
./rabbit test
./rabbit apk dev --debug
./rabbit apk test --release
```

模拟器调试：

```bash
./rabbit devices
./rabbit run dev -d Pixel_8_Pro_API_34
```

如果有多个设备，先用 `flutter devices` 确认 device id。

## 验收规则

- Flutter 代码改动默认至少运行 `./rabbit analyze`。
- 业务逻辑、provider、model、API wrapper 改动应运行 `./rabbit test`。
- Android 构建配置或依赖改动应运行 `./rabbit apk dev --debug`。
- 纯规则或文档改动不要求运行 Flutter 构建，但要检查 `.rule`、`AGENTS.md`、`CLAUDE.md` 指向关系。

## 迁移策略

- Flutter 首版是并行客户端，不删除原生 Android 页面。
- 对未迁移功能保留清晰入口占位，不隐藏用户预期中的主流程。
- 迁移稳定后，替换包名、迁移 NFC / 通知 / 离线缓存、下线原生 App 需要单独计划。
