<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.mapper.CageMapper">
    <resultMap id="CageMap" type="com.rabbit.app.model.Cage">
        <id column="id" property="id"/>
        <result column="house_id" property="houseId"/>
        <result column="cage_number" property="cageNumber"/>
        <result column="status" property="status"/>
        <result column="rabbit_count" property="rabbitCount"/>
        <result column="is_fed" property="isFed"/>
        <result column="is_enabled" property="isEnabled"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insertBatch">
        insert into cages (house_id, cage_number, status, rabbit_count, is_fed, is_enabled, remark, create_by, update_by)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.houseId}, #{item.cageNumber}, #{item.status}, #{item.rabbitCount}, #{item.isFed}, #{item.isEnabled}, #{item.remark}, #{item.createBy}, #{item.updateBy})
        </foreach>
    </insert>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into cages (house_id, cage_number, status, rabbit_count, is_fed, is_enabled, remark, create_by, update_by)
        values (#{houseId}, #{cageNumber}, #{status}, #{rabbitCount}, #{isFed}, #{isEnabled}, #{remark}, #{createBy}, #{updateBy})
    </insert>

    <select id="selectByHouseId" resultMap="CageMap">
        select id, house_id, cage_number, status, rabbit_count, is_fed, is_enabled, remark, create_by, create_time, update_by, update_time
        from cages
        where house_id = #{houseId}
        order by id asc
    </select>

    <select id="selectById" resultMap="CageMap">
        select id, house_id, cage_number, status, rabbit_count, is_fed, is_enabled, remark, create_by, create_time, update_by, update_time
        from cages
        where house_id = #{houseId} and id = #{id}
    </select>

    <update id="updateBasic">
        update cages
        set cage_number = #{cageNumber},
            remark = #{remark},
            is_enabled = #{isEnabled},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <update id="setRabbitCount">
        update cages
        set rabbit_count = #{rabbitCount},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <delete id="deleteById">
        delete from cages
        where house_id = #{houseId} and id = #{id}
    </delete>

    <update id="updateRabbitCountAndStatus">
        update cages
        set rabbit_count = #{rabbitCount},
            status = #{status},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <update id="incRabbitCount">
        update cages
        set rabbit_count = rabbit_count + #{delta},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <update id="updateIsFed">
        update cages
        set is_fed = #{isFed},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <update id="resetAllFed">
        update cages
        set is_fed = false,
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and is_fed = true
    </update>
</mapper>
