<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.mapper.TreatmentRecordMapper">
    <resultMap id="Map" type="com.rabbit.app.model.TreatmentRecord">
        <id column="id" property="id"/>
        <result column="house_id" property="houseId"/>
        <result column="rabbit_id" property="rabbitId"/>
        <result column="start_date" property="startDate"/>
        <result column="diagnosis" property="diagnosis"/>
        <result column="drug" property="drug"/>
        <result column="dose" property="dose"/>
        <result column="days" property="days"/>
        <result column="next_review_date" property="nextReviewDate"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="request_id" property="requestId"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into treatment_records (house_id, rabbit_id, start_date, diagnosis, drug, dose, days, next_review_date, status, remark, request_id, create_by, update_by)
        values (#{houseId}, #{rabbitId}, #{startDate}, #{diagnosis}, #{drug}, #{dose}, #{days}, #{nextReviewDate}, #{status}, #{remark}, #{requestId}, #{createBy}, #{updateBy})
    </insert>

    <select id="selectById" resultMap="Map">
        select id, house_id, rabbit_id, start_date, diagnosis, drug, dose, days, next_review_date, status, remark, request_id, create_by, create_time, update_by, update_time
        from treatment_records
        where house_id = #{houseId} and id = #{id}
    </select>

    <select id="selectByReq" resultMap="Map">
        select id, house_id, rabbit_id, start_date, diagnosis, drug, dose, days, next_review_date, status, remark, request_id, create_by, create_time, update_by, update_time
        from treatment_records
        where house_id = #{houseId} and rabbit_id = #{rabbitId} and request_id = #{requestId}
        limit 1
    </select>

    <update id="updateStatus">
        update treatment_records
        set status = #{status},
            update_by = #{updateBy},
            update_time = now()
        where house_id = #{houseId} and id = #{id}
    </update>

    <select id="selectByRabbit" resultMap="Map">
        select id, house_id, rabbit_id, start_date, diagnosis, drug, dose, days, next_review_date, status, remark, request_id, create_by, create_time, update_by, update_time
        from treatment_records
        where house_id = #{houseId} and rabbit_id = #{rabbitId}
        order by id desc
        limit #{limit}
    </select>

    <select id="selectDueReviewsByHouse" resultMap="Map">
        select id, house_id, rabbit_id, start_date, diagnosis, drug, dose, days, next_review_date, status, remark, request_id, create_by, create_time, update_by, update_time
        from treatment_records
        where house_id = #{houseId}
          and status = 'OPEN'
          and next_review_date is not null
          and next_review_date &lt;= #{now}
        order by next_review_date asc, id asc
    </select>
</mapper>
