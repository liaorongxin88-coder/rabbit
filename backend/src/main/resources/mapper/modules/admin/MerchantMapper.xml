<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.modules.admin.mapper.MerchantMapper">
    <resultMap id="MerchantMap" type="com.rabbit.app.modules.admin.entity.Merchant">
        <id column="id" property="id"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="name" property="name"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="FilterWhere">
        <where>
            <if test="keyword != null and keyword != ''">
                and (
                    name like concat('%', #{keyword}, '%')
                    or contact_name like concat('%', #{keyword}, '%')
                    or contact_phone like concat('%', #{keyword}, '%')
                )
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into merchants (name, contact_name, contact_phone, status, remark, create_by, update_by)
        values (#{name}, #{contactName}, #{contactPhone}, #{status}, #{remark}, #{createBy}, #{updateBy})
    </insert>

    <select id="selectById" resultMap="MerchantMap">
        select id, owner_user_id, name, contact_name, contact_phone, status, remark, create_by, create_time, update_by, update_time
        from merchants
        where id = #{id}
    </select>

    <select id="selectPage" resultMap="MerchantMap">
        select id, owner_user_id, name, contact_name, contact_phone, status, remark, create_by, create_time, update_by, update_time
        from merchants
        <include refid="FilterWhere"/>
        order by id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countPage" resultType="long">
        select count(1)
        from merchants
        <include refid="FilterWhere"/>
    </select>

    <update id="updateBasic">
        update merchants
        set name = #{name},
            contact_name = #{contactName},
            contact_phone = #{contactPhone},
            remark = #{remark},
            update_by = #{updateBy},
            update_time = now()
        where id = #{id}
    </update>

    <update id="updateStatus">
        update merchants
        set status = #{status},
            update_by = #{updateBy},
            update_time = now()
        where id = #{id}
    </update>

    <update id="updateOwner">
        update merchants
        set owner_user_id = #{ownerUserId},
            update_by = #{updateBy},
            update_time = now()
        where id = #{id}
    </update>
</mapper>
