<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.modules.sale.mapper.SaleOrderItemMapper">
    <resultMap id="Map" type="com.rabbit.app.modules.sale.entity.SaleOrderItem">
        <id column="id" property="id"/>
        <result column="sale_order_id" property="saleOrderId"/>
        <result column="rabbit_id" property="rabbitId"/>
        <result column="weight" property="weight"/>
        <result column="price" property="price"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insertBatch">
        insert into sale_order_items (sale_order_id, rabbit_id, weight, price, create_by, update_by)
        values
        <foreach collection="list" item="it" separator=",">
            (#{it.saleOrderId}, #{it.rabbitId}, #{it.weight}, #{it.price}, #{it.createBy}, #{it.updateBy})
        </foreach>
    </insert>

    <select id="selectByOrder" resultMap="Map">
        select id, sale_order_id, rabbit_id, weight, price, create_by, create_time, update_by, update_time
        from sale_order_items
        where sale_order_id = #{saleOrderId}
        order by id asc
    </select>

    <select id="selectViewByOrder" resultType="com.rabbit.app.modules.sale.dto.SaleOrderItemView">
        select i.id                as id,
               i.sale_order_id     as saleOrderId,
               i.rabbit_id         as rabbitId,
               r.cage_id           as cageId,
               r.type              as type,
               r.gender            as gender,
               i.weight            as weight,
               i.price             as price
        from sale_order_items i
                 inner join sale_orders o on o.id = i.sale_order_id
                 left join rabbits r on r.id = i.rabbit_id
        where o.house_id = #{houseId}
          and i.sale_order_id = #{saleOrderId}
        order by i.id asc
    </select>
</mapper>
