<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.modules.batch.mapper.BatchCarcassYieldMapper">
    <resultMap id="Map" type="com.rabbit.app.modules.batch.entity.BatchCarcassYieldVersion">
        <id column="id" property="id"/>
        <result column="house_id" property="houseId"/>
        <result column="batch_id" property="batchId"/>
        <result column="yield_rate" property="yieldRate"/>
        <result column="source_unit" property="sourceUnit"/>
        <result column="measured_date" property="measuredDate"/>
        <result column="report_number" property="reportNumber"/>
        <result column="evidence_file" property="evidenceFile"/>
        <result column="remark" property="remark"/>
        <result column="change_reason" property="changeReason"/>
        <result column="request_id" property="requestId"/>
        <result column="payload_hash" property="payloadHash"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into batch_carcass_yield_versions (
            house_id, batch_id, yield_rate, source_unit, measured_date,
            report_number, evidence_file, remark, change_reason,
            request_id, payload_hash, created_by
        ) values (
            #{houseId}, #{batchId}, #{yieldRate}, #{sourceUnit}, #{measuredDate},
            #{reportNumber}, #{evidenceFile}, #{remark}, #{changeReason},
            #{requestId}, #{payloadHash}, #{createdBy}
        )
    </insert>

    <select id="selectByRequestId" resultMap="Map">
        select *
        from batch_carcass_yield_versions
        where house_id = #{houseId} and request_id = #{requestId}
        limit 1
    </select>

    <select id="selectPage" resultMap="Map">
        select *
        from batch_carcass_yield_versions
        where house_id = #{houseId} and batch_id = #{batchId}
        order by created_at desc, id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countByBatch" resultType="long">
        select count(1)
        from batch_carcass_yield_versions
        where house_id = #{houseId} and batch_id = #{batchId}
    </select>
</mapper>
