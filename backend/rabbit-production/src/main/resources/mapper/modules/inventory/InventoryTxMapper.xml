<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.app.modules.inventory.mapper.InventoryTxMapper">
    <resultMap id="Map" type="com.rabbit.app.modules.inventory.entity.InventoryTx">
        <id column="id" property="id"/>
        <result column="house_id" property="houseId"/>
        <result column="item_id" property="itemId"/>
        <result column="tx_type" property="txType"/>
        <result column="qty_delta" property="qtyDelta"/>
        <result column="tx_time" property="txTime"/>
        <result column="ref_table" property="refTable"/>
        <result column="ref_id" property="refId"/>
        <result column="remark" property="remark"/>
        <result column="request_id" property="requestId"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into inventory_txs (house_id, item_id, tx_type, qty_delta, tx_time, ref_table, ref_id, remark, request_id, create_by, update_by)
        values (#{houseId}, #{itemId}, #{txType}, #{qtyDelta}, #{txTime}, #{refTable}, #{refId}, #{remark}, #{requestId}, #{createBy}, #{updateBy})
    </insert>

    <select id="selectPageByItem" resultMap="Map">
        select id, house_id, item_id, tx_type, qty_delta, tx_time, ref_table, ref_id, remark, request_id, create_by, create_time, update_by, update_time
        from inventory_txs
        where house_id = #{houseId} and item_id = #{itemId}
        order by tx_time desc, id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="selectExportPage" resultMap="Map">
        select id, house_id, item_id, tx_type, qty_delta, tx_time, ref_table, ref_id, remark, request_id, create_by, create_time, update_by, update_time
        from inventory_txs
        where house_id = #{houseId} and item_id = #{itemId}
        <if test="from != null">
            and tx_time <![CDATA[>=]]> #{from}
        </if>
        <if test="to != null">
            and tx_time <![CDATA[<=]]> #{to}
        </if>
        order by tx_time desc, id desc
        limit #{limit} offset #{offset}
    </select>
</mapper>
