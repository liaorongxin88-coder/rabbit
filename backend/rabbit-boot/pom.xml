<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.rabbit</groupId>
        <artifactId>rabbit-backend</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>rabbit-boot</artifactId>

    <properties>
        <spring-boot.maven.skip>false</spring-boot.maven.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.rabbit</groupId>
            <artifactId>rabbit-platform</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rabbit</groupId>
            <artifactId>rabbit-access</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rabbit</groupId>
            <artifactId>rabbit-production</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rabbit</groupId>
            <artifactId>rabbit-reporting</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- spring-boot-starter-test 现由父 pom 统一提供。archunit 只有这里用得到：
             架构测试要 importPackages("com.rabbit.app")，必须站在能看见全部模块的位置。 -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>rabbit-backend</finalName>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
            <testResource>
                <directory>${maven.multiModuleProjectDirectory}/src/test/resources</directory>
                <includes>
                    <include>fixtures/**/*.sql</include>
                </includes>
            </testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                    <mainClass>com.rabbit.app.RabbitBackendApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
