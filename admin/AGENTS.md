# admin/AGENTS.md

This file is a compatibility entrypoint for agent tooling.

Read and follow `.rules` first:

```text
admin/.rules
```

For any visible UI, layout, copy, motion, Tailwind token, shadcn-style component composition, responsive behavior, empty state, dialog, table, form, or status presentation change, also read and follow:

```text
admin/DESIGN.md
```

Do not duplicate durable instructions here. Update `.rules` for engineering rules and `DESIGN.md` for visual or interaction design rules.
